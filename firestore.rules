rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Firestore tidak dipakai sebagai primary DB (Supabase PostgreSQL).
      // Deny all by default untuk menghindari exposure legacy.
      allow read, write: if false;
    }
  }
}
